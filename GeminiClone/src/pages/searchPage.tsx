import { memo } from "react";

function SearchPage() {
    return (
        <main className="max-w-2xl mx-auto min-h-screen mt-20 transition-all duration-300 ease-in-out">
            <h1 className="text-2xl text-white font-semibold">Search</h1>
        </main>
    )
}
export default memo(SearchPage);